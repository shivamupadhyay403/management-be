const express = require('express');
const router = express.Router();
// const commonController = require("../controllers/commonController");
// const validateUserToken = require("../middleware/validateUserMiddleware");

// router.get("/common/all-users", validateUserToken, commonController.getUsers);
// router.get("/messages/:userId", validateUserToken, commonController.getMessages);

module.exports = router;
